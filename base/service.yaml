apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginxloadbalander
spec:
  selector:
    app: nginxdeployment
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 30008
  type: NodePort
